[phases.setup]
nixPkgs = ["php82", "php82Extensions.mysqli", "php82Extensions.pdo_mysql", "php82Extensions.session", "php82Extensions.mbstring", "php82Extensions.gd"]

[phases.build]
cmds = ["mkdir -p /app/uploads/coe /app/uploads/valid_id /app/uploads/proof_of_billing"]

[start]
cmd = "php -S 0.0.0.0:$PORT /app/index.php"
